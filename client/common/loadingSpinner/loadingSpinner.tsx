import * as React from "react";

class LoadingSpinner extends React.PureComponent {
    public render(): JSX.Element {
        return (
            <div>
                <div> Insert loading spinner or text here. </div>
            </div>
        );
    }
}

export default LoadingSpinner;
